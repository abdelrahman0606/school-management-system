fastapi==0.115.6
uvicorn[standard]==0.34.0
pydantic==2.10.4
torch==2.13.0+cpu
# EXPERIMENTAL on this branch: transformers 5.5.0, with the fix for the
# all_tied_weights_keys crash applied in main.py (DesklibAIDetectionModel).
# Do NOT merge this to dev/main until re-verified with the same two-tier
# essay smoke test used to confirm the 4.47.1 pin actually works: a
# genuinely AI-style essay should score ~100/likely_ai_generated=true, a
# genuinely human one with a normal personal voice should score well below
# 50/false. If that split doesn't hold (e.g. both score high/low/identical,
# or scores are suspiciously flat regardless of input), the checkpoint's
# real weights are likely not loading correctly under this version even
# though it no longer crashes -- revert to transformers==4.47.1 instead.
transformers==5.5.0
safetensors==0.4.5
